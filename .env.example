# ML Model MCP Server Configuration
# Copy this file to .env and customize as needed

# Directory where trained models are stored
MODELS_DIR=saved_models

# Server configuration (for HTTP transport)
MCP_HOST=127.0.0.1
MCP_PORT=8080
